# Copiez en api/config/.env sur le serveur FTP (ne pas committer api/config/.env)
# Ionos → Bases de données → dbs15772578

DB_HOST=db5020658636.hosting-data.io
DB_PORT=3306
DB_NAME=dbs15772578
DB_USER=dbu977482
DB_PASSWORD="VOTRE_MOT_DE_PASSE_IONOS"
DB_CHARSET=utf8mb4

APP_ENV=production
JWT_SECRET=changez-moi-en-production-64-caracteres-minimum

# Tests insertion BDD — GET /api/health/insert/run?key=...
INSERT_TEST_KEY=nexytal-insert-test
